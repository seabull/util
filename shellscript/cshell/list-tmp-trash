#!/bin/csh -f
#------------------------------------------------------------------------------

  cd /usr/tmp

  find . \( -user $LOGNAME -a -name '.[A-Za-z]*' -type f \) -print |& \
    egrep -v "(^find:|do not allow)"

#------------------------------------------------------------------------------
