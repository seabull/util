#!/bin/csh -f
#------------------------------------------------------------------------------

  set src = ~/doc/pl-sql-keywords.lst
  set dst = ~/doc/pl-sql-keywords.st

  sort -u < $src | x-case | awk '{ printf "%s|\\\n", $1 }' > $dst

#------------------------------------------------------------------------------
