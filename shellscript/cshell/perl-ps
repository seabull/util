#!/bin/csh -f
#------------------------------------------------------------------------------

  while ( $#argv > 0 )
    echo "--- $1"
    pod2man `perldoc -l $1` | groff -man -Tps | psnup -2 | lp
    shift
  end

#------------------------------------------------------------------------------
