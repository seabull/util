#!/bin/csh -f
#------------------------------------------------------------------------------

  if ( "$1" == "-print" ) then
    shift
    stty cols 80
    setenv PAGER cat
    perldoc -t $*
    stty cols 112
  else
    perldoc $*
  endif

#------------------------------------------------------------------------------
